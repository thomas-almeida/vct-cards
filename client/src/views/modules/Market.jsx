export default function Market(props) {

    return (
        <>
            <div className={props.visible ? `block` : `hidden`}>
                <h1>Market</h1>
            </div>
        </>
    )
}