export default function OnlineLeague(props) {

    return (
        <>
            <div className={props.visible ? `block` : `hidden`}>
                <h1>OnlineLeague</h1>
            </div>
        </>
    )
}